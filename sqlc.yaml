version: "2"

sql:
  - engine: "postgresql"
    schema: "database/schema"
    queries: "database/queries"
    gen:
      go:
        package: "sqlc"
        out: "internal/sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: false